export interface WorkExperience {
  position: string;
  company: string;
  period: string;
  location: string;
  description: string;
  tasks: Array<string>;
}

export interface Education {
  diploma: string;
  institution: string;
  period: string;
  location: string;
  description: string;
}
